module.exports = [{
  file: '',
  versions: '1.0.x',
  intercept: (myVersion) => {
    if (typeof myVersion === 'object') {
      throw new Error(`You're likely using my-version v2!`);
    }
    return `${myVersion}-patched`;
  }
}, {
  file: 'lib/patch.js',
  versions: '1.1.x',
  intercept: (myVersion) => {
    if (typeof myVersion === 'object') {
      throw new Error(`You're likely using my-version v2!`);
    }
    return `${myVersion}-patched`;
  }
}];
