module.exports = [{
  versions: '*',
  patch: (myVersion) => {
    if (typeof myVersion !== 'object') {
      throw new Error(`You're likely using my-version v1!`);
    }
    myVersion.version = '2.0.0-patched';
  }
}];
